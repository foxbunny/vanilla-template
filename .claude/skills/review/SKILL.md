---
name: review
description: Check changed files against the house rules and report violations
argument-hint: [files or globs, e.g. "public/*.js" — defaults to your changes]
---

Claude Code wrapper. Follow the portable steps in **`skills/review.md`**. Where
that body says "the argument", use `$ARGUMENTS` (the files or globs to review).
