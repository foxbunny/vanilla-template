---
name: add-icon
description: Add an icon to the sprite so <x-icon name="…"> can use it
argument-hint: <icon-name> (kebab-case, e.g. "calendar")
---

Claude Code wrapper. Follow the portable steps in **`skills/add-icon.md`**. Where
that body says "the argument", use `$ARGUMENTS` (the icon name, kebab-case).
