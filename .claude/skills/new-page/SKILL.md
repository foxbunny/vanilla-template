---
name: new-page
description: Start a new page by copying the canonical example trio
argument-hint: <page-name> (e.g. "settings" → settings.html/.js/.css)
---

Claude Code wrapper. Follow the portable steps in **`skills/new-page.md`**. Where
that body says "the argument", use `$ARGUMENTS` (the page name, kebab-case).
